# 四种 UDS 0x29 认证方案实验结果与分析

## 实验环境与可比性

- Tongsuo 源码提交：`540603a3f`，Tongsuo 8.5.0-pre2，底层 OpenSSL 3.5.4；
- 编译：GCC `-O2`，Tongsuo 默认 aarch64 汇编优化；
- 平台：Linux aarch64，Apple 虚拟化 CPU，固定 CPU 0；
- 执行：预热 1,000 轮，正式 10,000 轮，32 字节随机 challenge；
- 功能结果：四种方案正向认证全部成功，篡改 challenge 全部拒绝。

密钥生成、KGC 部分私钥提取和证书签发不计入在线时间。三种证书方案每次会话均解析 DER 叶证书、验证单级证书链、检查证书 CN 与 UUID 一致，再验证 `UUID || chal` 的签名。SM2 挑战签名采用 SM3 和标准 16 字节 SM2 用户 ID。

虚拟化环境存在少量调度长尾，均值和标准差会被极端暂停放大。因此以下比较以中位数为主，P95 表示常见尾延迟；完整均值和标准差保存在 `results/apple-virtualized-aarch64/results.csv`。

## 在线计算耗时

单位为微秒，表内数据为 `中位数 / P95`。

| 方案 | 2901 凭据处理 | 6901 challenge | 2903 签名 | 6903 验签 | 四阶段合计 |
|---|---:|---:|---:|---:|---:|
| CL-ECS-SM2 | 138.334 / 162.875 | 0.333 / 1.125 | 122.292 / 145.208 | 256.375 / 302.750 | **518.626 / 617.043** |
| RSA-2048-PSS+X509 | 23.084 / 32.500 | 0.375 / 1.042 | 340.417 / 457.625 | 16.125 / 20.917 | **380.582 / 521.167** |
| ECDSA-P256+X509 | 50.750 / 66.458 | 0.417 / 1.167 | 12.875 / 16.708 | 34.750 / 42.167 | **99.209 / 124.876** |
| SM2-SM3+X509 | 156.958 / 187.792 | 0.333 / 1.000 | 130.750 / 154.750 | 136.584 / 161.541 | **425.750 / 509.499** |

### 新增 SM2 证书方案的表现

- SM2 证书方案完整在线认证中位数为 425.750 μs，比 CL-ECS 快 17.9%，但比 RSA 慢 11.9%，约为 ECDSA-P256 的 4.29 倍。
- SM2 挑战签名为 130.750 μs，与 CL-ECS 的 122.292 μs 接近（慢 6.9%），比 RSA 签名快 61.6%，但约为优化 ECDSA-P256 的 10.16 倍。
- SM2 验签为 136.584 μs，比 CL-ECS 验签快 46.7%。原因是标准 SM2 验签只处理一套长期公钥关系，而本实验的无证书 ECS 还要完成其 Schnorr 型组合公钥验证方程。
- SM2 的 0x2901 凭据处理为 156.958 μs，是三种证书方案中最高的。其叶证书由 SM2 根签名，每会话证书链验证本身需要一次 SM2 证书签名验证；RSA 证书验证受益于较小公钥指数，ECDSA 则受益于高度优化的 P-256 路径。

### 四方案总体排序

按完整在线耗时中位数，由快到慢为：

```text
ECDSA-P256 (99.209) < RSA-2048 (380.582) < SM2 (425.750) < CL-ECS (518.626) μs
```

若证书已被安全缓存，只比较 0x2903 签名与 0x6903 验签，中位数合计为：ECS 378.667 μs、RSA 356.542 μs、ECDSA 47.625 μs、SM2 267.334 μs。此时 SM2 优于 RSA 和 ECS，但仍明显慢于平台优化的 ECDSA-P256。

从 ECU 计算负担看，0x2901 加 0x6903 的中位数分别约为 ECS 394.709 μs、RSA 39.209 μs、ECDSA 85.500 μs、SM2 293.542 μs。无证书方案减少了证书传输和 PKI 处理，但并不等于减少 ECU 端椭圆曲线运算。

## 通信开销

字节数包含 UDS SID、子功能和结果字段，不包含 ISO-TP/CAN 头。ECDSA 与 SM2 使用本轮观测到的最大 72 字节 DER 签名。

| 方案 | 2901 | 6901 | 2903 | 6903 | 合计 |
|---|---:|---:|---:|---:|---:|
| CL-ECS-SM2 | 84 | 34 | 67 | 3 | **188** |
| RSA-2048-PSS+X509 | 760 | 34 | 258 | 3 | **1,055** |
| ECDSA-P256+X509 | 365 | 34 | 74 | 3 | **476** |
| SM2-SM3+X509 | 364 | 34 | 74 | 3 | **475** |

SM2 叶证书 DER 为 346 字节，与 ECDSA 的 347 字节基本相同；两种 ECC 证书方案的通信开销可视为同一量级。SM2 相比 RSA 减少约 55.0% 的负载，但比无证书 ECS 多 287 字节。反过来说，ECS 相比 SM2 证书方案减少 60.4% 的认证负载。ECC 证书和 DER 签名含随机长度字段，不同进程生成时可能相差 1–2 字节。

按普通寻址作简化 ISO-TP 估算：

| 方案 | 经典 CAN 帧 | CAN FD 帧 |
|---|---:|---:|
| CL-ECS-SM2 | 29 | 6 |
| RSA-2048-PSS+X509 | 152 | 20 |
| ECDSA-P256+X509 | 70 | 10 |
| SM2-SM3+X509 | 70 | 10 |

该估算使用经典 CAN 首帧 6 字节、连续帧 7 字节，以及 CAN FD 单帧约 62 字节、连续帧约 63 字节，未计流控帧、扩展寻址与 padding。

## 论文结论建议

新增 SM2 基线后，可以把结论表述为：

1. 无证书 ECS 的稳定优势是最小通信负载和无需在线传输 X.509 证书；在经典 CAN 上这一优势最明显。
2. 标准 SM2+证书在通信量上与 ECDSA+证书近似，在计算上优于 CL-ECS 和 RSA 的完整挑战签名路径，但每会话 SM2 证书验证使其 0x2901 成本较高。
3. ECDSA-P256 在当前 aarch64/Tongsuo 软件平台上计算性能最佳，主要受益于专用 P-256 优化；该结论不能直接外推到只具备国密加速器的目标 ECU。
4. “无证书”降低的是证书传输、存储和生命周期管理成本，并不保证密码运算更快。论文应分别评价计算延迟、总线占用和 PKI/密钥管理复杂度。

最终工程选择取决于约束：带宽极紧张且希望简化证书分发时，CL-ECS 更有吸引力；要求国密合规且可接受证书传输时，SM2+X.509 是更标准化的路径；以纯软件计算延迟为首要目标时，本平台上的 ECDSA-P256 最优。
