# GM/T 0130-2023 CL-ECS-SM2 诊断认证实验报告

> 数据生成时间：2026-08-25 11:36（UTC+8）
>
> 测试设备：Apple M4 Mac mini，16 GB，macOS 15.2，arm64
>
> 样本规模：预热 1,000 次，正式测量 10,000 次
>
> 原始结果：[results.csv](results/wangguilin-wangguilindemac-mini-darwin-arm64/results.csv) · [results.txt](results/wangguilin-wangguilindemac-mini-darwin-arm64/results.txt) · [metadata.json](results/wangguilin-wangguilindemac-mini-darwin-arm64/metadata.json)

## 1. 实验目的与实现状态

本实验比较四种可用于 UDS `0x29 Authentication` 原型的认证方案：

- `CL-ECS-SM2`：按 GM/T 0130-2023 实现的无证书公钥机制与 SM2 签名；
- `RSA-X509`：RSA 2048 位证书与签名；
- `ECDSA-X509`：NIST P-256 证书与 ECDSA 签名；
- `SM2-X509`：SM2 证书与签名。

当前 CL-ECS-SM2 已不再使用早期的自定义 Schnorr 流程。实现采用标准中的无证书密钥生成关系：

1. 计算 `HA = SM3(ENTL || ID || a || b || xG || yG || xPpub || yPpub)`；
2. KGC 生成 `W = wG + U`，计算 `lambda = SM3(xW || yW || HA) mod n`；
3. 生成部分私钥 `t = w + lambda * ms mod n`，用户合成私钥 `d = t + d' mod n`；
4. 用户实际公钥为 `P = W + lambda * Ppub = dG`；
5. 在线认证使用标准 SM2 签名与验签，签名在线路中固定编码为 64 字节 `r || s`。

执行 `make test` 后，附录 A 的 `Ppub`、`HA`、`WA`、`dA`、`PA` 和签名 `(r,s)` 均与参考数据一致，篡改消息测试也被正确拒绝。因此，本报告的性能数据建立在经过标准参考向量验证的实现上。

## 2. 测试环境与方法

| 项目 | 配置 |
|---|---|
| 处理器 | Apple M4，10 核（4 性能核 + 6 能效核） |
| 内存 | 16 GB LPDDR5 |
| 操作系统 | macOS 15.2，Darwin 24.2.0，arm64 |
| 编译器 | Apple clang 16.0.0 |
| 编译选项 | `-O2 -g -std=c11 -Wall -Wextra -Wpedantic` |
| 密码库 | Tongsuo 8.5.0-pre2 / OpenSSL 3.5.4 |
| CPU 亲和性 | 未绑定 |
| 测量方法 | 单进程串行执行；每种方案预热 1,000 次，测量 10,000 次 |

每轮测量分为四个阶段：

- `credential`：生成或编码认证凭据；
- `challenge`：生成 32 字节随机挑战；
- `sign`：对挑战与上下文组成的消息签名；
- `verify`：解析凭据并验证签名；
- `total`：上述完整在线认证路径。

时间单位为微秒。由于 `challenge` 非常短，当前计时分辨率下其中位数显示为 `0 μs`；这表示小于一个微秒刻度，不表示该步骤没有执行。

## 3. 计算性能结果

### 3.1 各阶段延迟

| 方案 | 阶段 | 均值（μs） | 中位数（μs） | P95（μs） | 标准差（μs） |
|---|---|---:|---:|---:|---:|
| CL-ECS-SM2 | credential | 132.310 | 130 | 135 | 28.822 |
| CL-ECS-SM2 | challenge | 0.205 | 0 | 1 | 0.587 |
| CL-ECS-SM2 | sign | 133.703 | 131 | 137 | 26.182 |
| CL-ECS-SM2 | verify | 140.065 | 137 | 143 | 26.014 |
| **CL-ECS-SM2** | **total** | **406.283** | **399** | **415** | **68.678** |
| RSA-X509 | credential | 24.512 | 23 | 26 | 10.524 |
| RSA-X509 | challenge | 0.225 | 0 | 1 | 0.632 |
| RSA-X509 | sign | 327.716 | 318 | 375 | 64.445 |
| RSA-X509 | verify | 16.604 | 16 | 17 | 6.133 |
| **RSA-X509** | **total** | **369.056** | **359** | **429** | **73.015** |
| ECDSA-X509 | credential | 52.032 | 50 | 55 | 15.451 |
| ECDSA-X509 | challenge | 0.238 | 0 | 1 | 0.546 |
| ECDSA-X509 | sign | 13.560 | 13 | 14 | 4.157 |
| ECDSA-X509 | verify | 35.517 | 35 | 36 | 7.849 |
| **ECDSA-X509** | **total** | **101.347** | **99** | **105** | **24.354** |
| SM2-X509 | credential | 150.182 | 147 | 154 | 25.589 |
| SM2-X509 | challenge | 0.212 | 0 | 1 | 0.581 |
| SM2-X509 | sign | 131.214 | 129 | 134 | 26.805 |
| SM2-X509 | verify | 131.945 | 129 | 135 | 21.432 |
| **SM2-X509** | **total** | **413.553** | **406** | **423** | **63.712** |

完整数据见：[阶段延迟图](figures/wangguilin-wangguilindemac-mini-darwin-arm64/latency_by_phase.svg)和[延迟—载荷权衡图](figures/wangguilin-wangguilindemac-mini-darwin-arm64/latency_payload_tradeoff.svg)。

### 3.2 结果解读

以完整认证路径的中位数为准：

- CL-ECS-SM2 为 `399 μs`，比 SM2-X509 的 `406 μs` 快约 `1.7%`，两者处于同一性能水平；
- CL-ECS-SM2 比 RSA-X509 的 `359 μs` 慢约 `11.1%`；
- ECDSA-X509 最快，中位数为 `99 μs`；CL-ECS-SM2 约为其 `4.03` 倍；
- CL-ECS-SM2 的 P95 为 `415 μs`，P95/中位数约为 `1.04`。

CL-ECS-SM2 三个主要阶段的中位数分别为 `130 μs`、`131 μs` 和 `137 μs`。按三者之和 `398 μs` 计算，credential、sign、verify 分别占约 `32.66%`、`32.91%` 和 `34.42%`。因此，早期报告中“验签阶段占近一半、构成单一瓶颈”的判断不再适用于标准实现；当前开销在凭据生成、签名和验签之间基本均衡。

## 4. 通信开销结果

### 4.1 UDS 载荷组成

| 方案 | 凭据（B） | 挑战（B） | 签名（B） | 控制字段（B） | 总载荷（B） |
|---|---:|---:|---:|---:|---:|
| **CL-ECS-SM2** | **51** | **34** | **66** | **3** | **154** |
| RSA-X509 | 760 | 34 | 258 | 3 | 1,055 |
| ECDSA-X509 | 365 | 34 | 74 | 3 | 476 |
| SM2-X509 | 364 | 34 | 74 | 3 | 475 |

CL-ECS-SM2 的 51 字节凭据由 16 字节 UUID、33 字节压缩点 `W` 和 2 字节 UDS 服务字段组成。签名使用固定 64 字节 `r || s`，加 2 字节服务字段后为 66 字节。X.509 方案的证书和 DER 签名长度与本次生成的具体证书及签名有关。

与其他方案相比，CL-ECS-SM2 总载荷减少：

- 相比 SM2-X509：减少 `321 B`，约 `67.6%`；
- 相比 ECDSA-X509：减少 `322 B`，约 `67.6%`；
- 相比 RSA-X509：减少 `901 B`，约 `85.4%`。

载荷构成见：[线字节对比图](figures/wangguilin-wangguilindemac-mini-darwin-arm64/payload_size.svg)。

### 4.2 ISO-TP 分帧估算

| 方案 | Classical CAN 帧数 | CAN FD 帧数 |
|---|---:|---:|
| **CL-ECS-SM2** | **24** | **5** |
| RSA-X509 | 152 | 20 |
| ECDSA-X509 | 70 | 10 |
| SM2-X509 | 69 | 10 |

该表按项目中的 ISO-TP 载荷模型估算，只反映协议载荷导致的分帧数量，不包含总线竞争、流控等待、填充、链路层重传和 ECU 调度时间。图表见：[ISO-TP 帧数图](figures/wangguilin-wangguilindemac-mini-darwin-arm64/isotp_frames.svg)。

## 5. 综合结论

本次标准化修改改变了旧报告的主要结论。

在 Apple M4 软件实现上，GM/T 0130-2023 CL-ECS-SM2 的完整认证中位延迟为 `399 μs`，与 SM2-X509 的 `406 μs` 基本相当，同时将 UDS 总载荷从 `475 B` 降至 `154 B`，减少约 `67.6%`。因此，相对于同为 SM2 曲线体系的 X.509 基线，当前方案不再表现为“以明显计算开销换取通信空间”，而是表现为“计算延迟近似不变、通信开销显著下降”。

但这一优势不能泛化为对所有传统方案都更快：RSA-X509 在本机上快约 `11.4%`，ECDSA-X509 明显更快。CL-ECS-SM2 的核心收益仍然是凭据紧凑、签名长度固定以及 ISO-TP 分帧显著减少；是否能转化为更短的车载诊断端到端时间，还需要在真实 ECU、CAN/CAN FD 总线及完整 UDS 会话中验证。

## 6. 与旧报告数据的关系

旧版报告基于 Linux x86_64 环境及自定义 ECS/Schnorr 流程，记录过 `1,693.511 μs` 的中位延迟、`188 B` 的总载荷以及“验签是主要瓶颈”等结论。这些数字描述的是不同算法、不同实现和不同硬件，已不能代表当前 GM/T 0130-2023 实现，也不能与本次结果直接相除后解释为优化比例。

仓库中其他设备目录若尚未使用当前标准代码重新执行，应视为历史数据。跨设备汇总或 `docs/MULTI_DEVICE_ANALYSIS.md` 在所有目标设备完成重测前，不应作为当前标准实现的总体性能结论。

## 7. 有效性限制与后续工作

- 当前只有一台 Apple M4 设备的一轮正式结果，尚未形成跨设备、跨编译器的重复实验；
- CPU 未绑定，采样时系统负载不为零，调度和频率变化会影响尾延迟；
- 测量对象是主机上的密码操作与内存编码，不是真实 ECU 的端到端 UDS 时延；
- X.509 凭据尺寸取决于证书字段、扩展和编码，本报告数值仅对应本次生成配置；
- 附录 A 测试证明关键算法步骤与标准参考数据一致，但不能替代独立安全审计和协议互操作测试。

建议下一步在相同代码提交上重跑全部目标设备，并记录 CPU 亲和性、性能/节能模式和多轮置信区间；随后再更新跨设备分析，并在 CAN 与 CAN FD 实车或台架上测量完整 `0x29` 会话时延。
