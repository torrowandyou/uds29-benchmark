# GM/T 0130-2023 CL-ECS-SM2 诊断认证实验报告

> 数据生成时间：2026-08-25 15:12（UTC+8）
>
> 测试设备：香港 KVM VPS，2 vCPU，2 GB，Ubuntu 24.04.4，x86_64
>
> 样本规模：预热 1,000 次，正式测量 10,000 次
>
> 原始结果：[results.csv](results/root-c202605211665597-linux-x86_64/results.csv) · [results.txt](results/root-c202605211665597-linux-x86_64/results.txt) · [metadata.json](results/root-c202605211665597-linux-x86_64/metadata.json)

## 1. 实验目的与实现状态

本实验比较四种可用于 UDS `0x29 Authentication` 原型的认证方案：

- `CL-ECS-SM2`：按 GM/T 0130-2023 实现的无证书公钥机制与 SM2 签名；
- `RSA-3072-PSS+X509`：RSA 3072 位证书与 RSA-PSS/SHA-256 签名；
- `ECDSA-X509`：NIST P-256 证书与 ECDSA 签名；
- `SM2-X509`：SM2 证书与签名。

本实验按约 128 位经典安全强度选择主对比参数。依据 [NIST SP 800-57 Part 1 Rev.5](https://doi.org/10.6028/NIST.SP.800-57pt1r5) 的安全强度对应表，256–383 位 ECC 与 3072 位 RSA 均处于约 128 位安全强度档；RSA-2048 约为 112 位安全强度。因此，本轮以 RSA-3072 替代 RSA-2048，避免因安全强度不一致而使 RSA 性能基线偏低。

当前 CL-ECS-SM2 已不再使用早期的自定义 Schnorr 流程。实现采用标准中的无证书密钥生成关系：

1. 计算 `HA = SM3(ENTL || ID || a || b || xG || yG || xPpub || yPpub)`；
2. KGC 生成 `W = wG + U`，计算 `lambda = SM3(xW || yW || HA) mod n`；
3. 生成部分私钥 `t = w + lambda * ms mod n`，用户合成私钥 `d = t + d' mod n`；
4. 用户实际公钥为 `P = W + lambda * Ppub = dG`；
5. 在线认证使用标准 SM2 签名与验签，签名在线路中固定编码为 64 字节 `r || s`。

执行 `make test` 后，附录 A 的 `Ppub`、`HA`、`WA`、`dA`、`PA` 和签名 `(r,s)` 均与参考数据一致，篡改消息测试也被正确拒绝。因此，本报告的性能数据建立在经过标准参考向量验证的实现上。

## 2. 测试环境与方法

| 项目 | 配置 |
|---|---|
| 处理器 | 2 vCPU，Intel Xeon Platinum 8272CL @ 2.60 GHz |
| 内存 | 2 GB |
| 虚拟化 | KVM |
| 操作系统 | Ubuntu 24.04.4 LTS，Linux 6.8.0-137-generic，x86_64 |
| 编译器 | GCC 13.3.0 |
| 编译选项 | `-O2 -g -std=c11 -Wall -Wextra -Wpedantic` |
| 密码库 | Tongsuo 8.5.0-pre2 / OpenSSL 3.5.4 |
| Benchmark 提交 | `65afb38`，采集时工作区干净 |
| CPU 亲和性 | 未绑定 |
| 测量方法 | 单进程串行执行；每种方案预热 1,000 次，测量 10,000 次 |

每轮测量分为四个阶段：

- `credential`：解析并验证认证凭据；ECS 派生实际公钥，X.509 方案验证证书链并提取公钥；
- `challenge`：生成 32 字节随机挑战；
- `sign`：对挑战与上下文组成的消息签名；
- `verify`：解析凭据并验证签名；
- `total`：上述完整在线认证路径。

时间单位为微秒。`challenge` 阶段非常短，VPS 上各方案的中位数约为 `1.020–3.113 μs`，因此该阶段对总耗时的影响可以忽略，但不表示该步骤没有执行。

## 3. 计算性能结果

### 3.1 各阶段延迟

| 方案 | 阶段 | 均值（μs） | 中位数（μs） | P95（μs） | 标准差（μs） |
|---|---|---:|---:|---:|---:|
| CL-ECS-SM2 | credential | 446.125 | 430.395 | 509.130 | 67.394 |
| CL-ECS-SM2 | challenge | 1.530 | 1.020 | 3.032 | 17.243 |
| CL-ECS-SM2 | sign | 449.148 | 432.534 | 517.685 | 80.247 |
| CL-ECS-SM2 | verify | 420.496 | 406.287 | 484.696 | 52.455 |
| **CL-ECS-SM2** | **total** | **1,317.298** | **1,277.675** | **1,475.083** | **157.465** |
| RSA-3072-PSS+X509 | credential | 116.204 | 109.959 | 155.426 | 39.901 |
| RSA-3072-PSS+X509 | challenge | 2.212 | 1.841 | 4.170 | 1.571 |
| RSA-3072-PSS+X509 | sign | 1,944.876 | 1,870.636 | 2,585.137 | 264.815 |
| RSA-3072-PSS+X509 | verify | 67.768 | 63.655 | 86.275 | 37.559 |
| **RSA-3072-PSS+X509** | **total** | **2,131.059** | **2,050.834** | **2,770.765** | **290.132** |
| ECDSA-X509 | credential | 168.963 | 161.918 | 217.749 | 39.421 |
| ECDSA-X509 | challenge | 3.718 | 3.113 | 6.590 | 8.887 |
| ECDSA-X509 | sign | 35.010 | 32.759 | 47.612 | 14.318 |
| ECDSA-X509 | verify | 77.175 | 72.709 | 95.522 | 13.846 |
| **ECDSA-X509** | **total** | **284.865** | **274.147** | **354.569** | **53.068** |
| SM2-X509 | credential | 479.542 | 461.370 | 561.767 | 79.202 |
| SM2-X509 | challenge | 1.492 | 1.172 | 3.093 | 1.287 |
| SM2-X509 | sign | 447.281 | 430.171 | 516.300 | 81.381 |
| SM2-X509 | verify | 397.910 | 383.029 | 461.618 | 67.129 |
| **SM2-X509** | **total** | **1,326.225** | **1,284.739** | **1,511.751** | **171.951** |

完整数据见：[阶段延迟图](figures/root-c202605211665597-linux-x86_64/latency_by_phase.svg)和[延迟—载荷权衡图](figures/root-c202605211665597-linux-x86_64/latency_payload_tradeoff.svg)。

### 3.2 结果解读

以完整认证路径的中位数为准：

- CL-ECS-SM2 为 `1,277.675 μs`，SM2-X509 为 `1,284.739 μs`；ECS 快约 `0.55%`，可视为同一性能水平；
- CL-ECS-SM2 比 RSA-3072-PSS+X509 的 `2,050.834 μs` 快约 `37.7%`；RSA 的主要开销来自中位数为 `1,870.636 μs` 的签名阶段；
- ECDSA-X509 最快，中位数为 `274.147 μs`；CL-ECS-SM2 约为其 `4.66` 倍；
- CL-ECS-SM2 的 P95 为 `1,475.083 μs`，P95/中位数约为 `1.15`。

CL-ECS-SM2 三个主要阶段的中位数分别为 `430.395 μs`、`432.534 μs` 和 `406.287 μs`。按三者之和 `1,269.216 μs` 计算，credential、sign、verify 分别占约 `33.91%`、`34.08%` 和 `32.01%`。因此，早期报告中“验签阶段占近一半、构成单一瓶颈”的判断不再适用于标准实现；当前开销在凭据处理、签名和验签之间基本均衡。

## 4. 通信开销结果

### 4.1 UDS 载荷组成

| 方案 | 凭据（B） | 挑战（B） | 签名（B） | 控制字段（B） | 总载荷（B） |
|---|---:|---:|---:|---:|---:|
| **CL-ECS-SM2** | **51** | **34** | **66** | **3** | **154** |
| RSA-3072-PSS+X509 | 1,016 | 34 | 386 | 3 | 1,439 |
| ECDSA-X509 | 365 | 34 | 74 | 3 | 476 |
| SM2-X509 | 363 | 34 | 74 | 3 | 474 |

CL-ECS-SM2 的 51 字节凭据由 16 字节 UUID、33 字节压缩点 `W` 和 2 字节 UDS 服务字段组成。签名使用固定 64 字节 `r || s`，加 2 字节服务字段后为 66 字节。X.509 方案的证书和 DER 签名长度与本次生成的具体证书及签名有关。

与其他方案相比，CL-ECS-SM2 总载荷减少：

- 相比 SM2-X509：减少 `320 B`，约 `67.5%`；
- 相比 ECDSA-X509：减少 `322 B`，约 `67.6%`；
- 相比 RSA-3072-PSS+X509：减少 `1,285 B`，约 `89.3%`。

载荷构成见：[线字节对比图](figures/root-c202605211665597-linux-x86_64/payload_size.svg)。

### 4.2 ISO-TP 分帧估算

| 方案 | Classical CAN 帧数 | CAN FD 帧数 |
|---|---:|---:|
| **CL-ECS-SM2** | **24** | **5** |
| RSA-3072-PSS+X509 | 208 | 26 |
| ECDSA-X509 | 70 | 10 |
| SM2-X509 | 69 | 10 |

该表按项目中的 ISO-TP 载荷模型估算，只反映协议载荷导致的分帧数量，不包含总线竞争、流控等待、填充、链路层重传和 ECU 调度时间。图表见：[ISO-TP 帧数图](figures/root-c202605211665597-linux-x86_64/isotp_frames.svg)。

## 5. 综合结论

本次标准化修改改变了旧报告的主要结论。

在香港 x86_64 VPS 软件实现上，GM/T 0130-2023 CL-ECS-SM2 的完整认证中位延迟为 `1,277.675 μs`，与 SM2-X509 的 `1,284.739 μs` 基本相同，同时将 UDS 总载荷从 `474 B` 降至 `154 B`，减少约 `67.5%`。因此，相对于同为 SM2 曲线体系的 X.509 基线，当前方案表现为“计算延迟基本等价、通信开销显著下降”。

在约 128 位经典安全强度的 RSA 对比中，CL-ECS-SM2 还比 RSA-3072-PSS+X509 快约 `37.7%`，并将总载荷从 `1,439 B` 降至 `154 B`。不过，ECDSA-P256-X509 仍然明显更快。CL-ECS-SM2 的综合收益是相对 RSA-3072 同时降低计算与通信开销，并在与 SM2-X509 计算性能基本相当时显著减少凭据和 ISO-TP 分帧；是否能转化为更短的车载诊断端到端时间，还需要在真实 ECU、CAN/CAN FD 总线及完整 UDS 会话中验证。

## 6. 与旧报告数据的关系

旧版报告在同类 Linux x86_64 环境下使用自定义 ECS/Schnorr 流程，记录过 `1,693.511 μs` 的中位延迟、`188 B` 的总载荷以及“验签是主要瓶颈”等结论。这些数字描述的是不同算法和不同实现，已不能代表当前 GM/T 0130-2023 实现；即使测试设备相同，也不能把两次结果直接相除后全部解释为代码优化比例。

上一轮标准实现报告仍采用 RSA-2048，其约 112 位安全强度低于 256 位椭圆曲线的约 128 位安全强度，因此当时“RSA 比 ECS 快”的结论并非等安全强度比较。本轮升级到 RSA-3072 后，RSA 签名和通信开销显著增加，结论相应变为 ECS 在该 VPS 上比 RSA-3072 更快且更紧凑。两轮 RSA 数据应分别保留其密钥长度标签，不能混为同一基线。

本香港 VPS 已完成标准 ECS 与 RSA-3072 的等安全强度重测。Apple M4 目录中的 ECS 已采用标准实现，但 RSA 基线仍为 RSA-2048，因此不能纳入本轮 RSA-3072 对比；其他尚未重测的设备结果也应视为历史数据。由于硬件、操作系统、编译器和运行负载不同，不同设备的绝对延迟不应直接作为算法优劣依据。`docs/MULTI_DEVICE_ANALYSIS.md` 在所有目标设备完成 RSA-3072 重测前，也不应作为当前等安全强度方案的总体性能结论。

## 7. 有效性限制与后续工作

- 当前只有香港 x86_64 VPS 完成了 RSA-3072 等安全强度正式测量，且只有一轮结果，尚未形成跨设备重复实验和多轮置信区间；
- VPS 只有 2 个虚拟 CPU，CPU 未绑定；虚拟机调度和宿主机争用可能影响尾延迟；
- 测量对象是主机上的密码操作与内存编码，不是真实 ECU 的端到端 UDS 时延；
- X.509 凭据尺寸取决于证书字段、扩展和编码，本报告数值仅对应本次生成配置；
- NIST 对应关系描述的是经典计算条件下的估计安全强度，不代表量子计算条件下的安全性，也不消除不同算法实现质量和硬件加速造成的差异；
- 附录 A 测试证明关键算法步骤与标准参考数据一致，但不能替代独立安全审计和协议互操作测试。

建议下一步在相同代码提交上重跑全部目标设备，并记录 CPU 亲和性、性能/节能模式和多轮置信区间；随后再更新跨设备分析，并在 CAN 与 CAN FD 实车或台架上测量完整 `0x29` 会话时延。
