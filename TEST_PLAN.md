# 测试方案

## 1. 目标与假设

目标是比较同一 Tongsuo、同一主机和同一 32 字节 UDS 挑战下，CL-ECS-SM2、RSA-2048、ECDSA-P256 和 SM2/SM3 证书四种 0x29 认证方案的在线计算延迟、通信开销和认证正确性。

- H1：CL-ECS 不传 X.509 证书，0x2901 和总通信量显著小于传统方案。
- H2：CL-ECS 的签名比 RSA-2048 快，但其完整验签包含多次通用 SM2 曲线标量乘，未必快于标准 SM2、RSA 验签或优化的 P-256 ECDSA。
- H3：在每会话验证证书的模型下，证书解析/验证会增加 RSA/ECDSA/SM2 的 0x2901 成本；是否改变端到端排序由证书签名和挑战签名算法共同决定。

## 2. 变量和控制

自变量为 `CL-ECS-SM2`、`RSA-2048-PSS+X509`、`ECDSA-P256+X509`。因变量为四阶段耗时、四报文及总字节数、成功/失败判定。固定条件包括同一进程、相同 Tongsuo 构建、`-O2`、32 字节 challenge、压缩椭圆曲线点、单级 CA、相同预热/迭代次数和单 CPU 亲和性。

每轮生成新挑战和新签名随机数。长期密钥及证书在进程启动时生成一次，模拟已完成车辆生产/工具注册。

## 3. 用例

| ID | 用例 | 预期 |
|---|---|---|
| F-01 | 四种方案完成 2901→6901→2903→6903 | 10,000/10,000 成功 |
| F-02 | 签名后翻转 challenge 一位 | 四种方案全部拒绝 |
| P-01 | 测量 2901 凭据处理 | 输出 mean/median/P95/SD |
| P-02 | 测量 6901 随机挑战 | 输出 mean/median/P95/SD |
| P-03 | 测量 2903 签名 | 输出 mean/median/P95/SD |
| P-04 | 测量 6903 验签 | 输出 mean/median/P95/SD |
| S-01 | 统计实际 DER 证书及签名长度 | 输出每报文和总字节数 |

建议论文扩展用例：错误 UUID、替换 `X/R`、无效/无穷远曲线点、截断签名、过期证书、错误 CA、证书公钥替换、challenge 重放、并发诊断会话、不同总线负载和目标 ECU/HSM 实测。

## 4. 执行和判定

先执行 1,000 轮预热，再执行 10,000 轮计时；四种方案按轮次旋转先后顺序。功能用例必须全部通过，否则不接受性能数据。性能不设固定通过阈值，以中位数为主、P95 为实时性参考，均值和标准差用于呈现调度噪声。

至少在空闲主机重复 5 次，报告每次中位数及其 95% 置信区间；最终应在目标 ECU 编译器、CPU 频率、Tongsuo 配置和硬件加速条件下复测。当前结果只可作为该 aarch64 软件实现的对照实验。
